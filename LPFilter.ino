Quaternion LPFilter(Quaternion* qx, Quaternion* qxn1, Quaternion* qyn1) {
  //=======================================================
  //======            FUNCTION Variables            =======
  //=======================================================
  float a1 = 0;
  float b0 = 0;
  float b1 = 0;

  //Deconstruct Quaternion vecotr
  for (unsigned int i = 0; i < sizeof(qx) / sizeof(unsigned int)) {

    // Apply filter on extracted measured data
    Quaternion qy;
  }

  //ay Container for empty filtered quaternion vector
  //qx raw measured quaternion vector

  qv return
}